Este directorio fue generado mediante `expo prebuild --platform android` para crear el proyecto nativo Android y su Gradle Wrapper (`./gradlew`).

Se incluye para permitir que validaciones/análisis automatizados que ejecutan tareas Gradle no fallen por ausencia de `gradlew`.
